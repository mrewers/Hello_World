let balance;
const threshold;
const phonePrice;
const accessPrice;
const taxRate = 0.1;

for ( var totalPrice = 0; totalPrice <= money;   )

function taxCalc(totalPrice) {
  totalAmount = totalPrice * (totalPrice * taxRate);
  return totalAmount;
}

function format() {

}
